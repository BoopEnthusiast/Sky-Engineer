extends Node


const INVENTORY: Dictionary[Array, PackedScene] = { # List of classes (in string form) that are required in the recipes you can make in the inventory, then the preload of the item 
	
}
